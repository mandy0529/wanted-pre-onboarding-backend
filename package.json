{
  "name": "wanted-pre-onboarding-backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "migrate:dev":"dotenv -e .env.dev -- prisma migrate dev",
    "migrate:deploy":"prisma migrate deploy",
    "migrate:test": "dotenv -e .env.test -- prisma migrate deploy",
    "generate":"prisma generate",

    "start:dev":"yarn migrate:dev && sleep 1 && nodemon src/server.js",
    "start:prod":"yarn migrate:deploy && sleep 1 && node src/server.js",
    "start:test": "yarn migrate:test && sleep 1 && mocha tests/*"
  },
  "type": "module",
  "dependencies": {
    "@prisma/client": "4.10.1",
    "prisma": "4.10.1",
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.1",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "dotenv": "^16.3.1",
    "dotenv-cli": "^7.2.1",
    "express-async-errors": "^3.1.1",
    "http-status-codes": "^2.2.0",
    "mocha": "^10.2.0",
    "morgan": "^1.10.0",
    "nodemon": "^3.0.1",
    "supertest": "^6.3.3",
    "validator": "^13.9.0"
  }
}
